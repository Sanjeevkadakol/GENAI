#program 3:To implement an image generator from user-provided textual prompts using Stable Diffusion. The
# program should allow users to experiment with different styles, lighting, and negative prompts to control
# the output.

!pip install diffusers transformers accelerate -q

from diffusers import StableDiffusionPipeline
import torch

pipe = StableDiffusionPipeline.from_pretrained(
    "runwayml/stable-diffusion-v1-5",
    torch_dtype=torch.float32)   #.to("cuda") this is optional

image = pipe("A beautiful sunset").images[0]

image.save("image.png")

image.show()
display(image)